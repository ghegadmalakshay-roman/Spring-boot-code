
server:
  port: 8080

spring:
  application:
    name: employee-management

  datasource:
    url: jdbc:mysql://localhost:3306/mcs_emp_management?useSSL=false&serverTimezone=UTC
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

